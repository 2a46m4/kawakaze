# Base FreeBSD 15.0-RELEASE image
# This is the base layer for all other images
FROM scratch

# Bootstrap FreeBSD base system
# Downloads and installs the complete FreeBSD base system
# Use auto-detection for version and architecture
BOOTSTRAP

# Set working directory
WORKDIR /root

# This image is bootstrapped with FreeBSD base system
# Build using: kawakaze build --name freebsd-15.0-release Dockerfile.base
